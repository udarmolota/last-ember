import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.lastember.game',
  appName: 'Last Ember',
  webDir: 'dist',
  android: {
    backgroundColor: '#ede9e0',
  },
}

export default config
